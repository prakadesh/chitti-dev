<Application x:Class="ClipEnhancer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ClipEnhancer">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Global Styles -->
            <Style TargetType="Button">
                <Setter Property="Margin" Value="0,5"/>
                <Setter Property="Padding" Value="15,8"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <ScrollViewer x:Name="PART_ContentHost"
                                            Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Colors -->
            <Color x:Key="PrimaryBackgroundColor">#FFFFFF</Color>
            <Color x:Key="CardBackgroundColor">#F8F9FA</Color>
            <Color x:Key="PrimaryTextColor">#1A1A1A</Color>
            <Color x:Key="SecondaryTextColor">#666666</Color>
            <Color x:Key="AccentBlueColor">#2563EB</Color>
            <Color x:Key="SuccessGreenColor">#22C55E</Color>
            <Color x:Key="ErrorRedColor">#EF4444</Color>

            <!-- Brushes -->
            <SolidColorBrush x:Key="PrimaryBackground" Color="{StaticResource PrimaryBackgroundColor}"/>
            <SolidColorBrush x:Key="CardBackground" Color="{StaticResource CardBackgroundColor}"/>
            <SolidColorBrush x:Key="PrimaryText" Color="{StaticResource PrimaryTextColor}"/>
            <SolidColorBrush x:Key="SecondaryText" Color="{StaticResource SecondaryTextColor}"/>
            <SolidColorBrush x:Key="AccentBlue" Color="{StaticResource AccentBlueColor}"/>
            <SolidColorBrush x:Key="SuccessGreen" Color="{StaticResource SuccessGreenColor}"/>
            <SolidColorBrush x:Key="ErrorRed" Color="{StaticResource ErrorRedColor}"/>
        </ResourceDictionary>
    </Application.Resources>
</Application> 